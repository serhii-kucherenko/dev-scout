recipient: ""  # set via DEV_SCOUT_EMAIL (preferred) or DELIVERY_TO
mode: draft
subject_template: "Dev Scout {day} — {top_count} ways to ship faster / build safer"

# Email drafts are written to runs/YYYY-MM-DD/06-email/ as .md, .json, and .eml files.
# Set DEV_SCOUT_EMAIL in GitHub Actions secrets/vars (or local .env) for the To: address.
# No send API keys required for draft mode.
