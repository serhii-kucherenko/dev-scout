recipient: kucherenko.web@gmail.com
mode: draft
subject_template: "Dev Scout {week} — {top_count} ways to ship faster / build safer"

# Email drafts are written to runs/YYYY-Www/06-email/ as .md, .json, and .eml files.
# No send integration is required for the weekly loop. No API keys required.
