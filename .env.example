# Recipient for daily jam email drafts (To: header).
# Add the same name as a GitHub Actions variable or secret.
DEV_SCOUT_EMAIL=

# Optional aliases / send path (CI helpers only — not required for the chat loop)
# DELIVERY_TO=
# DELIVERY_FROM=
# RESEND_API_KEY=
